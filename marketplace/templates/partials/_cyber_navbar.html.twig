<style>
    /* --- SINGLE NAVBAR CYBER --- */
    .cyber-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 25px 40px;
        background: rgba(45, 20, 80, 0.7); /* Violet transparent */
        backdrop-filter: blur(15px);
        border-bottom: 1px solid rgba(139, 61, 255, 0.4);
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 1000;
        font-family: 'Courier New', monospace;
    }

    /* Left: Logo & User Info */
    .header-logo {
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    .nav-logo {
        height: 40px;
        width: auto;
        object-fit: contain;
    }

    .user-tag-left {
        text-align: left;
        margin-top: 5px;
    }

    .user-name-nav {
        display: block;
        color: #ff0080;
        font-size: 11px;
        font-weight: bold;
        letter-spacing: 1px;
        text-transform: uppercase;
    }

    .user-role-nav {
        display: block;
        color: rgba(255, 255, 255, 0.4);
        font-size: 8px;
        letter-spacing: 1px;
        text-transform: uppercase;
    }

    /* Center: Nav Links */
    .header-nav {
        flex: 2;
        display: flex;
        justify-content: center;
        gap: 40px;
    }

    .nav-link {
        color: rgba(255, 255, 255, 0.6);
        text-decoration: none;
        text-transform: uppercase;
        font-size: 12px;
        font-weight: bold;
        letter-spacing: 2px;
        transition: all 0.3s ease;
        position: relative;
        padding: 5px 0;
    }

    .nav-link:hover, .nav-link.active {
        color: #fff !important;
        text-shadow: 0 0 8px rgba(139, 61, 255, 1);
    }

    .nav-link.active::after {
        content: "";
        position: absolute;
        bottom: -5px;
        left: 0;
        width: 100%;
        height: 2px;
        background: #8B3DFF;
        box-shadow: 0 0 10px #8B3DFF;
    }

    /* Right: Buttons */
    .header-user-nav {
        flex: 1;
        display: flex;
        justify-content: flex-end;
        align-items: center;
        gap: 15px;
    }

    .profile-dropdown {
        position: relative;
        display: inline-block;
    }

    .btn-profile-nav {
        background: rgba(139, 61, 255, 0.1);
        border: 1px solid rgba(139, 61, 255, 0.5);
        color: rgba(255, 255, 255, 0.8);
        padding: 8px 20px;
        font-size: 11px;
        font-weight: bold;
        letter-spacing: 2px;
        text-transform: uppercase;
        text-decoration: none;
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .dropdown-content {
        display: none;
        position: absolute;
        right: 0;
        top: 100%;
        background: rgba(25, 10, 45, 0.95);
        backdrop-filter: blur(10px);
        min-width: 180px;
        box-shadow: 0 8px 32px rgba(0,0,0,0.5);
        border: 1px solid rgba(139, 61, 255, 0.3);
        z-index: 1001;
        margin-top: 10px;
        border-radius: 4px;
        overflow: hidden;
    }

    .dropdown-content a {
        color: rgba(255, 255, 255, 0.8);
        padding: 12px 16px;
        text-decoration: none;
        display: block;
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: all 0.2s ease;
        border-bottom: 1px solid rgba(139, 61, 255, 0.1);
    }

    .dropdown-content a:last-child {
        border-bottom: none;
    }

    .dropdown-content a:hover {
        background: rgba(139, 61, 255, 0.2);
        color: #fff;
        padding-left: 20px;
    }

    .profile-dropdown:hover .dropdown-content {
        display: block;
    }

    .profile-dropdown:hover .btn-profile-nav {
        background: rgba(139, 61, 255, 0.2);
        color: #fff;
        border-color: #8B3DFF;
    }

    .btn-dissoudre-nav {
        background: transparent;
        border: 1px solid rgba(255, 0, 128, 0.5);
        color: rgba(255, 0, 128, 0.8);
        padding: 8px 15px;
        font-size: 11px;
        font-weight: bold;
        letter-spacing: 2px;
        text-transform: uppercase;
        text-decoration: none;
        transition: all 0.3s ease;
    }

    .btn-dissoudre-nav:hover {
        background: rgba(255, 0, 128, 0.1);
        color: #ff0080;
        border-color: #ff0080;
        box-shadow: 0 0 15px rgba(255, 0, 128, 0.3);
    }

    /* Adjust content padding for fixed header */
    .lobby-content {
        padding-top: 100px !important;
    }
</style>

<header class="cyber-header">
    <div class="header-logo">
        <a href="{{ path('app_home') }}">
            <img src="/assets/images/lobby-logo-white.png" alt="LOBBY.GG" class="nav-logo">
        </a>
        <div class="user-tag-left">
            <span class="user-name-nav">{{ app.user.prenom|default('USER')|upper }}</span>
            <span class="user-role-nav">
                {% if app.user and 'ROLE_SPONSOR' in app.user.roles %}LECTEUR (SPONSOR){% else %}JOUEUR (CLIENT){% endif %}
            </span>
        </div>
    </div>

    <nav class="header-nav">
        <a href="{{ path('app_home') }}" class="nav-link {{ app.request.get('_route') == 'app_home' ? 'active' : '' }}">HOME</a>
        <a href="{{ path('front_marketplace') }}" class="nav-link {{ app.request.get('_route') == 'front_marketplace' ? 'active' : '' }}">MARKET</a>
        <a href="{{ path('front_tournaments') }}" class="nav-link {{ app.request.get('_route') == 'front_tournaments' ? 'active' : '' }}">TOURNOI</a>
            
            {% if is_granted('ROLE_SPONSOR') %}
                <a href="{{ path('front_sponsoring_my_offers') }}" class="nav-link {{ app.request.get('_route') == 'front_sponsoring_my_offers' ? 'active' : '' }}">MES OFFRES</a>
                <a href="{{ path('front_sponsor_requests') }}" class="nav-link {{ app.request.get('_route') == 'front_sponsor_requests' ? 'active' : '' }}">DEMANDES</a>
                <a href="{{ path('front_contracts') }}" class="nav-link {{ app.request.get('_route') == 'front_contracts' ? 'active' : '' }}">CONTRATS</a>
            {% elseif is_granted('ROLE_CLIENT') %}
                <a href="{{ path('front_sponsoring') }}" class="nav-link {{ app.request.get('_route') == 'front_sponsoring' ? 'active' : '' }}">SPONSORING</a>
                <a href="{{ path('front_client_requests') }}" class="nav-link {{ app.request.get('_route') == 'front_client_requests' ? 'active' : '' }}">MES DEMANDES</a>
                <a href="{{ path('front_contracts') }}" class="nav-link {{ app.request.get('_route') == 'front_contracts' ? 'active' : '' }}">CONTRATS</a>
            {% endif %}
            
            {% if app.user %}
                {% set unread_count = app.user.notifications|filter(n => not n.isRead)|length %}
                <a href="{{ path('app_notifications') }}" class="nav-link {{ app.request.get('_route') == 'app_notifications' ? 'active' : '' }}">
                    NOTIFICATIONS{% if unread_count > 0 %} <span class="badge" style="background:#8B3DFF; margin-left:6px;">{{ unread_count }}</span>{% endif %}
                </a>
                <a href="{{ path('app_messages_index') }}" class="nav-link {{ app.request.get('_route') == 'app_messages_index' ? 'active' : '' }}">
                    MESSAGES
                    {% set unread_messages = 0 %}
                    {# On utilise l'injection de service ou une variable globale si possible, mais ici on va faire simple pour Twig #}
                    {# En Symfony, on pourrait passer cette variable via un EventListener ou un AppVariable personnalisé #}
                </a>
            {% endif %}
            <a href="{{ path('front_blog') }}" class="nav-link {{ app.request.get('_route') == 'front_blog' ? 'active' : '' }}">COMMUNITY</a>
    </nav>

    <div class="header-user-nav">
        <div class="profile-dropdown">
            <span class="btn-profile-nav">PROFILE</span>
            <div class="dropdown-content">
                <a href="{{ path('app_settings') }}"><i class="fa-solid fa-gear"></i> Settings</a>
                <a href="{{ path('app_profile_view') }}"><i class="fa-solid fa-user-astronaut"></i> View Profile</a>
            </div>
        </div>
        <a href="{{ path('app_logout') }}" class="btn-dissoudre-nav">DÉCONNEXION</a>
    </div>
</header>
